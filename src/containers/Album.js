import React from 'react'
import { MyConsumer } from '../MyContext'

function Album() {
    return (
        <MyConsumer>
            {context => }
        </MyConsumer>
    )
}

export default Album;
